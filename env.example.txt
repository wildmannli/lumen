# ============================================================
# Lumen Configuration
# Copy this file to .env and adjust values as needed.
#   cp .env.example .env
# ============================================================

# --- Server ---
PORT=8080
# Set to "production" for release mode (disables debug logs)
ENVIRONMENT=development

# --- Database ---
DATABASE_PATH=./lumen.db

# --- Paste Settings ---
MAX_PASTE_SIZE=524288           # Max paste size in bytes (default: 512 KB)
DEFAULT_EXPIRY=24h              # Default paste expiration (Go duration format)
MAX_EXPIRY=720h                 # Maximum paste expiration (default: 30 days)
ID_LENGTH=8                     # Length of generated paste IDs (min recommended: 6)

# --- Rate Limiting ---
CREATE_RATE_LIMIT=10            # Max paste creations per IP per window
VIEW_RATE_LIMIT=100             # Max paste views per IP per window
RATE_WINDOW=1h                  # Rate limit window (Go duration format)

# --- Reverse Proxy ---
# TRUSTED_PROXIES=127.0.0.1,10.0.0.0/8    # Comma-separated trusted proxy IPs/CIDRs

# --- Security ---
# IMPORTANT: Set this to a random string in production!
# Without it, IP hashes are unsalted and reversible via rainbow tables.
# Generate one with: openssl rand -hex 32
IP_HASH_SECRET=
